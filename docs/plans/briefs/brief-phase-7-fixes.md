# Phase 7 review fixes (run after Phase 8 lands; legacy stack is gone by then)

Read brief-common.md first. Set NODE_OPTIONS=--max-old-space-size=4096 for every node/pnpm command. Do not run `pnpm install` unless a package.json dependency changes. Stage explicit paths; commit with `-c core.hooksPath=/dev/null` only if the hook trips on files you did not touch.

Decisions already made (implement, don't re-litigate):
- Preview lane: switch `.github/workflows/preview.yml` to Workers Versions preview URLs on the single `gmacko-web-preview` Worker (`wrangler versions upload --env preview` after `migrate:remote`; post the preview URL on the PR; no per-PR Worker, no `--name`, no close-job deletion needed — keep a close job only if something else must be cleaned). This inherits the Worker's secrets/bindings. Phase 9's per-PR D1 task must revisit this (document in the workflow header). Update `packages/create-gmacko-app` scaffold tests that pin `wrangler deploy --env preview` / `gmacko-web-preview` strings.
- Single migrate owner under ForgeGraph: `.forgegraph.yaml` keeps `db.migrate` (deploy-stage --migrate-only) and stage `deploy` becomes `pnpm -F @gmacko/web deploy:<stage>` = plain `wrangler deploy --env <stage>` (no second migrate); `deploy/forgegraph/deploy.yml` runs migrate then deploy exactly once via `scripts/deploy-stage.mjs --stage`; fix the "single source" comment to name deploy-stage.mjs as the one place the ordering lives.
- Monitoring: wire apps/web to `@gmacko/monitoring` (`initSentryWeb` in client.tsx / `SentryErrorBoundary` in __root.tsx, `sentryWorkerOptions`/`withSentry` from `@gmacko/monitoring/web/server` in worker.ts), remove the duplicated option objects and the direct `@sentry/*` deps from apps/web if nothing else needs them (add `@gmacko/monitoring` to apps/web dependencies — this adds it to the web-bundle scope; make sure it passes the process.env rule). Keep the client bundle free of `@sentry/cloudflare` (assert with a build-output grep test or a vitest that imports the client entry graph).

Fixes:
1. `apps/web/scripts/dev-portless.mjs`: run `link-env.mjs` (import it) before spawning vite; add a test or at least a `--check` path that asserts `apps/web/.env` exists/links after the script's prep step. Update apps/web/README if wording changes.
2. `packages/telemetry/src/index.ts` (~33-41): `base: { service, version, ...options.logging?.base }`; test that passing `logging.base: { stage }` keeps `service`/`version`.
3. `packages/logging/src/index.ts` (~69-80, ~137): default redaction adds `*.authorization`, `*.cookie`, `*.set-cookie`; make `a.b.c` paths work (split on all dots, descend recursively) or reject them at construction with a clear error; core keys (`level`,`time`,`msg`) win over `fields` in `formatJsonLine`; tests for all three (update the test that pinned "nested it stays").
4. `apps/web/src/server/worker.ts`: `scheduled` wraps its effect so `flushTelemetry` runs under the controller's `waitUntil`; close the `TODO(Phase 7)` by adding pool-workers tests in apps/web for `withSentry`-wrapped `fetch` and `scheduled` (a minimal vitest.workers.config.ts like packages/db's; if TanStack Start's server entry cannot load under pool-workers, test a thin `makeWorker(handler)` factory instead and say so).
5. `apps/web/src/server/config.ts` (~14-17): require the provider secrets in staging/production (AUTH_SECRET, at least one OAuth pair, STRIPE_* when features.stripe) — `fromBindings` fails fast with a readable list of missing keys; test dev-permissive vs production-strict.
6. `packages/telemetry` `flushTelemetry`: 5-second timeout, failure ignored+logged.
7. `turbo.json`: move `CLOUDFLARE_ENV` and `SENTRY_AUTH_TOKEN` into `globalEnv` (they change build outputs).
8. `.github/workflows/preview.yml`: besides the Versions switch, run the migration in its own step and drop `cancel-in-progress` (or scope it so a migrate step is never cancelled).
9. `scripts/secrets-push.mjs`: pass `--config wrangler.jsonc` explicitly to `wrangler secret put`.
10. `packages/i18n/src/web/index.tsx`: memoise on locale + a stable messages reference (document), avoid the react-i18next "no instance" warning when the toggle is off (guard the hook usage with a provider check or a stub instance), load fallback-locale resources.
11. Standards rule (`scripts/check-app-standards.mjs`): include `peerDependencies`/`optionalDependencies` workspace refs in the web-bundle graph; comment why apps/web/src uses the app pattern (Vite replaces NODE_ENV).

Gates: `pnpm lint:ox`, `pnpm format:check`, `pnpm typecheck`, `pnpm check:standards`, `pnpm test:standards`, `pnpm test`, `pnpm test:workers`, `pnpm -F @gmacko/web build`, `pnpm -F @gmacko/web deploy:staging:dry-run`, `pnpm --dir packages/create-gmacko-app test`, and `pnpm e2e:web`. Conventional commits per area. Do not push, do not deploy. Report: each item done/skipped with evidence, test counts.
