# Migration execution tracker

Worktree: ~/.config/superpowers/worktrees/create-gmacko-app/migrate-tanstack-effect-d1
Branch: migrate/tanstack-effect-d1 (from 2ce0a91)
Plan: docs/plans/2026-09-02-tanstack-effect-d1-migration.html → https://ao98zs9lxlq2.postplan.dev

## Phase 0 — Spike & pin
- [x] Spike A PASS (commits ec78cea, 589ebbd, 6e85083): apps/web on CF Vite plugin; HttpApi via HttpRouter.toWebHandler; custom main src/server/worker.ts (fetch+scheduled, cron via GET /cdn-cgi/handler/scheduled); Background(waitUntil) proven; bundle 987 KiB gz (legacy deps inside); nodejs_compat default-on for compat_date>=2026-08-04 (Start + better-auth need node:async_hooks); .env only read from apps/web dir → dev script uses CLOUDFLARE_INCLUDE_PROCESS_ENV=true + with-env; shims for @gmacko/logging (pino) and @gmacko/telemetry (host-metrics) TODO-marked; stale 5432 listener hangs postgres over cloudflare:sockets. Review pending.
- [x] Legacy rename (76596d8): packages/{db,api,auth} → packages/legacy-*; all consumers + CI + scaffolder updated; tracer name became "@gmacko/legacy-api" (revert at Phase 8 deletion anyway); lint-staged EACCES in sandbox hook (checks run manually).
- [x] Spike A code review: 0 critical. Fix task queued after Spike B (same files): (1) background.ts Effect.ignore → Effect.ignoreCause(effect,{log:true,message}) and generic R; (2) remove 200ms spike from health live handler; route worker.ts scheduled through runtime.runPromise (exercise shared memoMap); (3) TODO-mark CLOUDFLARE_INCLUDE_PROCESS_ENV bridge in apps/web/package.json; minor: drop @cloudflare/workers-types dep+catalog, FileSystem.layerNoop once via provideMerge, `this.environment?.name` in vite shim, comment migrations_dir, deliberate fail-fast comment on Stage decode, decide HttpRouter logger. ADR must record: Sentry withSentry + crons deferred to Spike C; bundle 1,010,731 B gz at Spike A.
- [x] Spike B PASS (c39fb36, 5cfdbce, 1a36fc3): @gmacko/db on D1; 17 tables sqlite; relations; drizzle-kit rc5 + flatten script → migrations/20260903030938_init.sql; Database{db,sql,first,batch,updateWhere,ping,plain}; DatabaseError(reason); layerTest exported from @gmacko/db/testing (not Database.layerTest: node:sqlite can't be in the Worker-bundled module); Node 13 tests + pool-workers 11 tests green; /api/health/ready 200. Notes: rc5 effect-d1 ignores casing (explicit snake_case names); @effect/sql-d1 doesn't classify errors (message-text fallback); pool-workers 0.22 = `cloudflareTest` vite plugin, workerd compat cap 2026-08-22 → inline d1Databases config; apps/web ssr optimizeDeps.exclude drizzle-orm (two drizzle versions in workspace until Phase 8); batch rows are raw D1 rows. Import paths: drizzle-orm/effect-d1 makeWithDefaults, drizzle-orm/effect-sqlite-node, drizzle-orm/effect-core, drizzle-orm/sqlite-core/effect, @effect/sql-d1/D1Client, @effect/sql-sqlite-node/SqliteClient, effect/unstable/sql/{SqlClient,SqlError,Statement}.
- [x] Spike A fixes (83ba22c): ignoreCause+log, generic R, spike code removed, scheduled via runtime.runPromise (fiber #13 on shared runtime), README TODO for process.env bridge.
- [x] Spike B review: 0 critical. Queue for Phase 0 close-out: IMPORTANT (1) add `test:workers` turbo task + run in ci.yml; (2) export "./testing" from packages/db. MINOR: memoise `plain`; document/mapped helper for `sql` SqlError; updateWhere projection (`returning({n: sql\`1\`})`) + tighten GuardedWrite type; "no such table" → reason "other"/"schema"; workers vitest config → point at wrangler.jsonc or fix comment (pool-workers pins miniflare 5.20260815.0-alpha); explicit PRAGMA foreign_keys=ON in testing.ts + apply files via exec/assert one stmt per chunk; note created_at has no DB default; drop unused tsx, catalog vitest, knip ignore cloudflare:* + unused exports; flatten script stale-file check; schema test count tables from DB; caveat: mapper failures are defects.
- [x] Spike C PASS (1000ccf…469ba39): better-auth 1.7.2 (no pkg.pr.new), @gmacko/auth (genericOAuth github/google = first-class social providers → signIn.social; apple built-in), account.issuer migration, AppConfig from bindings (config.ts), AuthLive, /api/session/me, Observability (Otlp.layerJson baseUrl + Flusher via Background), withSentry, localTransport + HttpApiClient in / loader, crons. Cookies: better-auth.session_token/session_data/state; __Secure- only on https. Not verified: google/apple, oAuthProxy relay, real Sentry DSN, https cookie. Legacy: nextjs `as unknown as AuthPlugin`, expo await getCookie(), @better-auth/cli removed (better-call conflict).
- [x] ADR written docs/adr/0001-tanstack-effect-d1.md; plan Phase 0 marked Done, republished v5 (worktree path mapped in ~/.postplan). ADR + plan NOT yet committed (waiting for close-out agent to release the index).
- [x] Spike C review: 0 critical. Fix task queued: (1) bypassMagicLink only when STAGE=development (config.ts:130) else fail fromBindings; (2) local-transport.ts:17 forward only `cookie`; (3) issuer migration is pre-provisioning-only: say so in ADR + auth-schema header; add pool-workers test that the migration set applies from empty AND that a PRAGMA foreign_keys=OFF table rebuild with rows keeps session/account on D1; (4) emulate.config.yaml: add redirect_uris for http://localhost:3001/api/auth/callback/{github,google}; document AUTH_*_URL ports; (5) vitest: OAuth callback round trip with a fake token/userinfo server; OTLP export test with an in-process sink. Minor: emailNotWired message without the address; TODO on Effect.runPromise in lib/api.ts (Phase 4 runs through runtime); AdminOnly must bypass cookieCache (Phase 3 note); githubUserInfo early return when no token.
- [x] Phase 0 close-out (8175a85): test:workers in turbo + ci.yml; db fixes; bundle after Sentry 1,275,612 B gz total (index.js 211,450) — legacy chain still inside.
- [x] Docs commit f170083 (ADR + plan Phase 0 done).
- [x] Spike C review fixes (8ae66d3): bypass gated to development; transport forwards only cookie; OAuth callback + OTLP tests; emulate redirect_uris for localhost:3001. FINDING: D1 ignores PRAGMA foreign_keys=OFF in migration batches → drizzle-kit __new_ rebuilds cascade-delete rows. Expand/contract only from now on (ADR updated, uncommitted; Phase 2 brief adds standards rule no-d1-table-rebuild).

## Phase 1 — Domain contract — DONE (8ae66d3..9fec0dc): @gmacko/domain, 35 endpoints, 70 tests, docs/API_AUTH.md, HealthApi absolute paths added after .prefix, role-before-credential middleware order, SessionRecord model, Conflict reasons for state rules. Review running (incl. 8ae66d3). Plan marked done (uncommitted edit in worktree; republished).
- Phase 1 review: 0 critical; fixes landed 3f5db9f (waitlist-status-changed, securityIsOutermost test, API_AUTH.md "Implementation rules (Phase 3)" + "Decisions", domain tsconfig types:[] + tsconfig.scripts.json, OpenApi info gmacko/0.1.0). Phase 4 must-remember list folded into brief-phase-4.md.
## Phase 2 — DONE (2015826, f836aad, 5ae0c0e, c913fb4): standards rules + harness (pnpm test:standards in CI), seed (seedLocal Effect, seed.sql byte-stable, seed:local proven on D1), root db:* scripts (db:legacy:push kept for bootstrap-local.sh + e2e.yml), D1 docs + RUNBOOK, ADR committed. Plan marked done, committed 1ac6177, republished v7. Review running.
- Phase 2 review: 1 CRITICAL fixed in 6334e90 (bootstrap-local.sh guards D1 steps on apps/web/wrangler.jsonc), seed.sql drift test, stale db:push refs, runbook wording, no-d1-table-rebuild scans drizzle/ too.
## Phase 3 — Auth — DONE (4 commits after 6334e90): ApiKeys (WebCrypto sha256 parity), RequestContext (Cache per request; propagated via handler(request, context); middlewares serviceOption fallback), AuthSecurityConfig, SessionLive/SessionOrKeyLive/AdminOnlyLive/WorkspaceRoleLive/SecurityLive, 35 auth tests + 13 web tests. Decisions: any Authorization header = bearer credential; bearer on Session-only ⇒ 403 scope; lastUsedAt on authenticate even if later Forbidden(scope); DB failure in middleware = defect. Review running. Typecheck repair: f3e49d5 + da0cfa4 + 8c96c9b (seed drift test path string; Workers URL vs node URL type collision).
- Phase 3 review: 0 critical; fixes landed eaaa8ee (WorkspaceRole fail-closed via Object.hasOwn rank; membership lookup filters unknown roles; SessionOrKey single cookie path; best-effort lastUsedAt; orDie no double log; API_AUTH rule wording; auth 38/38). Plan marked Phase 3 done (95441a2, v8). Phase 4 agent added domain middleware declarations (RateLimit, EndpointBoundary) in 74a7b4d.
## Phase 4 — API — DONE (74a7b4d..1a63738): @gmacko/api 35/35 endpoints; 77 Node + 2 D1 tests; EndpointBoundary + RateLimit in domain; guarded conditional batches; apps/web server slimmed; lib/api.ts via ManagedRuntime. Decisions: payloads are Schema.Class instances (api-client hides it); listInvites 403; health shapes per domain models. Review running. Cookie-cache-after-delete fixed in e790243 (RequestContext.session validates user row via memoised users cache; deleteAccount calls auth.signOut and appends Max-Age=0 cookies; auth 40/40, api 77/77).
- Phase 4 review: 0 critical; 6 important → FIXED in 30211a8 (api 82/82, domain 72/72; rate-limit key user→key-hash→session-hash→ip→anon+warn; Retry-After; CORS trace headers; guarded insert-select launch controls; nullable prefs id/createdAt; idempotent approve, allowlist-exists dropped). Note: worktree node_modules vanished ~07:51 during Phase 6's install churn; restored with --prefer-offline.
- (old) fix agent running in packages/api + domain (4xx span status; identity-keyed rate limits + Retry-After; CORS traceparent; updateLaunchControls guarded insert-select; UserPreferences id/createdAt nullable; approve idempotent, drop allowlist-exists; minor). NOTE for Phase 5: forward cf-connecting-ip in FORWARDED_HEADERS (api-client transport) and add a local-transport test that the endpoint span parents on the SSR request.
- 2026-09-03: session rate limit (resets 4:10am Detroit) killed the Phase 4 fix agent and the Phase 6 agent mid-task; both RESUMED from the uncommitted working tree (fix: partial edits in packages/api+domain+auth; Phase 6: untracked packages/api-client scaffold).
## Phase 6 — Clients — DONE (a2734b7..0962008): @gmacko/api-client (transport cookie+cf-connecting-ip, queries, invalidation map, traceparent test), Expo ported, operator-core JSON schemas via Schema.toJsonSchemaDocument, api-cli rename, sdks from OpenApi.fromApi + drift check, trpc-client/validators deleted. Maestro not run. Plan marked done 0fba8f9, v10. Review running. ENV NOTE: shell NODE_OPTIONS has a stale --require preload; use NODE_OPTIONS=--max-old-space-size=4096 for node/pnpm.
- Phase 6 review: 0 critical; 3 important → FIXED 4f3fd0e (shouldRetry default; invalidation map complete + removal map; listUsers normalised keys + keepPreviousData; header provider failures = transport; tsconfig types:[] + tests tsconfig; api-client 39/39). Phase 5 told: server QueryClient retry:false.
- (old) fix agent in packages/api-client/src/queries (retry policy skipping typed 4xx; updateUserRole→admin.stats, waitlist→launchControls, remove→byId; listUsers key normalisation; header provider failure kind; tsconfig types:[]; stale refs). Phase 5 agent told to avoid those files + how to consume the client.
## Phase 5 — Web app — DONE (3e57cd7..4d19645): 10/10 pages; 34/34 Playwright (11 specs); CSP nonce via Start middleware; .env symlink predev; Storybook in packages/ui (10.6); Stripe webhook; tRPC gone from apps/web; e2e.yml web job on local D1. Known: @gmacko/settings#test legacy emulate-setup waits on :5432 (Phase 7 fixes); PostHog wired directly (analytics/web reads process.env → Phase 7); env.client.ts renamed env.ts (Start import-protection); plain.ts flattens Schema.Class for dehydrate; auth.$ route dispatches contract auth paths. Plan marked done 16e6d77, v11. Review running.
- Phase 5 review: 0 critical; 4 important → PostHog-vs-CSP handed to Phase 7 agent (owns providers.tsx); FIXED 3ba803c (e2e env allowlist, auth.$ ANY + template matching + 7 tests, delete-account navigate-only, plain.ts guards + 9 tests, api-key-secret story; web 39/39). PostHog-vs-CSP pending in Phase 7.
## Phase 7 — DONE (85d6be4..637b340, 18 commits; e2e 34/34, test 28/28, workers 13/13, deploy dry-run ok). Plan v12.
- Phase 7 review: 0 critical; 7 important → QUEUED after Phase 8 (file overlap): (1) dev-portless.mjs must run link-env first; (2) preview secrets — DECISION: switch preview.yml to Workers Versions preview URLs on the single `gmacko-web-preview` Worker (`wrangler versions upload`, inherits secrets/bindings, no per-PR Worker cleanup); Phase 9 revisits if per-PR D1 needs per-PR Workers; also close config.ts TODO (require provider secrets in staging/production); (3) telemetry Observability.layer merge `base: { service, version, ...options.logging?.base }` + test; (4) wire apps/web to @gmacko/monitoring (sentryWorkerOptions/initSentryWeb) or delete the entries — DECISION: wire it (the brief's deliverable); (5) scheduled handler flushes telemetry via controller waitUntil; (6) single migrate owner — DECISION: `.forgegraph.yaml` db.migrate only + `deploy: pnpm -F @gmacko/web deploy:<stage>` (plain wrangler deploy), deploy.yml calls migrate then deploy once; (7) logging default redaction adds *.authorization, *.cookie, *.set-cookie; a.b.c paths work or rejected. Minor: formatJsonLine core keys last; preview `--env preview` on redirected config; cancel-in-progress vs migrate step; turbo globalEnv CLOUDFLARE_ENV + SENTRY_AUTH_TOKEN; flushTelemetry 5s timeout; secrets-push `--config wrangler.jsonc`; i18n memo/warning.
## Phase 8 — Cutover — DONE on the branch (resumed from 9589081; merge to main is the user's call). Legacy dirs/docker/Next deps gone (lockfile needed no churn); scaffolder: PlatformConfig {web, mobile}, `--operator-lane` (was `--trpc-operators`), root `api:ops` (was `trpc:ops`), no vinext/tanstack-start flags, CLI version read from package.json, 52 unit tests, cli-e2e.yml matrix default/operators/minimal/scope/full/mobile-only, changeset minor (0.2.0); standards rules + 26 fixture tests unchanged from WIP and green repo-wide; docs sweep (README, ARCHITECTURE, DEVEX, SCAFFOLD_SPEC, IMPLEMENTATION_PLAN, DEPLOYMENT, RUNBOOK, PACKAGE_EXTRACTION, ALERTING, SECURITY, CONTRIBUTING, .opencode skills, repo skill; legacy plans moved to docs/legacy). FOUND+FIXED: `pnpm auth:generate` overwrote the hand-maintained auth-schema.ts with a v1 `relations()` schema (broke drizzle-kit and every generated app's bootstrap:local); it now writes packages/db/.cache/auth-schema.generated.ts. Gates: lint, format, typecheck, standards (26), test (23 tasks), test:workers (22), build, e2e:web 34/34, scaffolder 52/52, CLI e2e 25/26 + default re-run after the fix, knip, check:release. Next: brief-phase-7-fixes.md → Phase 8 review → brief-phase-9.md.
## (old) Phase 7 — 15 commits landed (85d6be4..392cc7f: logging/telemetry/monitoring/i18n/flags-analytics-config rewrites, standards scoping + no-dev-vars, settings test decoupled from Postgres, realtime Node-only, turbo env, dev = emulate + web under portless, migrate-then-deploy stage + secrets push, CI preview per PR + worker-types diff, docs, shims dropped, wrangler envs); Playwright run live; agent resumed to finish tests + report.
## Phase 4 — brief-phase-4.md ready
- [x] Research: API surface → scratchpad/api-surface.md. Key: drizzle-orm must be 1.0.0-rc.5-169397b (dist-tag rc5) + drizzle-kit 1.0.0-rc.5-ab785fc (rc.4 breaks on effect rc.112); no db.batch on Effect flavour → D1Client.batch with Statements (convert builders via toSQL + sql.unsafe); Model at effect/unstable/schema (GeneratedByApp/GeneratedByDb); web handler = HttpRouter.toWebHandler(HttpApiBuilder.layer(api) + Etag.layer + HttpPlatform.layer + FileSystem.layerNoop({}) + Path.layer); HttpClient.make((req,url,signal,fiber)=>...) + HttpClientRequest.toWeb/HttpClientResponse.fromWeb for in-process transport; Schema.toStandardSchemaV1; Effect.result (not either); drizzle-kit rc writes <out>/<ts>_<slug>/migration.sql → needs flatten step for wrangler; no migrator usable on Workers.
- [ ] Spike B: D1 + Drizzle Effect API + drizzle-kit migrations + Database service (db, first, batch, updateWhere, ping) + layerTest
- [ ] Spike C: better-auth 1.7.2 sqlite adapter on D1 (magic link + GitHub via emulate) + in-process transport + OTLP export
- [ ] Pins in catalog + ADR docs/adr/0001-tanstack-effect-d1.md with kill-criteria outcomes
- [ ] Update plan Phase 0 statuses, republish

## Phase 1 — Domain contract (after Phase 0)
Source: scratchpad/api-inventory.md (31 procedures: auth 2, post 4, settings 14, admin 11; 10 have no consumers). Contract decisions:
- createApiKey/revokeApiKey = SessionOrKey("admin") (operator CLI/MCP use keys; deviation from D2 list — flag to user); deleteAccount + completeBootstrap = Session only.
- Invalid/expired/revoked bearer key → 401 (no fall-through to cookie); lastUsedAt updated only when the key authenticates a request.
- updatePreferences: all fields optional, no defaults (fixes reset bug); getPreferences returns defaults without inserting; updatePreferences upserts.
- posts: create returns Post (returning), remove → 204 + 404 if missing, byId → 404, list ordered by createdAt desc, ids as path params.
- admin.listUsers.total = count(*).
- reviewWaitlistEntry: guarded write (Conflict if not pending) + allowlist insert idempotent; completeBootstrap guarded (D16); acceptInvite: lookup by (workspaceId,email) not full-table scan, batch membership+allowlist delete/update.
- deleteAccount cascade through workspace.ownerUserId kept, documented.
- Drop openapi.ts/versioning/deprecation/validators entirely (no consumers).
- getSession returns one User shape (with role) for both credential kinds.
## Phase 2 — D1 data layer (parallel with 1)
## Phase 3 — Auth on D1
## Phase 4 — API services
## Phase 5 — Web app  | Phase 6 — Clients (parallel)
## Phase 7 — Platform & CI
## Phase 8 — Scaffolder, docs, delete Next, cutover merge
## Phase 9 — Post-cutover hardening

## Decisions / discoveries during execution
- AUTH: pkg.pr.new pin e9a2a6d = tip of OPEN upstream PR #8814 (OAuth endpoint overrides: tokenEndpoint/userInfoEndpoint/jwksEndpoint), NOT in 1.7.2. Decision: better-auth 1.7.2 + genericOAuth plugin for github/google (authorizationUrl/tokenUrl/userInfoUrl from env with real defaults; callbacks /api/auth/callback/:id match emulate redirect_uris); apple = built-in social provider (authorizationEndpoint override only; emulate Apple web flow unsupported, documented). Spike C must verify oAuthProxy + genericOAuth interplay and Expo client (`signIn.oauth2` vs `signIn.social`; `getCookie()` now async). Drop both pkg.pr.new overrides (@better-auth/core too). CLI is now `npx auth@1.7.2 generate` (not @better-auth/cli); fake `drizzle({} as D1Database,{schema})` for config.
- AUTH ADAPTER: drizzleAdapter({provider:"sqlite", transaction:false}) on Database.plain (drizzle-orm/d1). 1.7.2 could also take raw D1Database (built-in kysely D1 dialect) — not used, schema stays drizzle-owned.
- OTLP: use `effect/unstable/observability` (Otlp.layer / OtlpTracer.layer etc., needs FetchHttpClient.layer from effect/unstable/http); flush via OtlpExporter.Flusher.flush; @effect/opentelemetry NOT needed. Sentry: withSentry((env)=>options, handler) instruments fetch+scheduled; skipOpenTelemetrySetup:true when running our own tracer.
- Cookies: `__Secure-` prefix when baseURL is https (or useSecureCookies); cookieCache {enabled,maxAge,strategy}.
- better-auth pkg.pr.new pin = 1.7.0-beta.2 (OAuth proxy + Expo fixes, upstream PR #1367); 1.7.2 published → target 1.7.2
- Migration branch cut from fix/mobile-e2e-ios-scheme HEAD (includes plan + Expo SDK56 fixes not yet on main)
