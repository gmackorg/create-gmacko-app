# Phase 9 brief: post-cutover hardening (lands after the Phase 8 cutover merge; on the same branch until then)

Read first: brief-common.md, api-surface.md (Model at effect/unstable/schema: Model.Class, GeneratedByApp/GeneratedByDb, DateTimeInsert/Update, variants insert/update/json/jsonCreate/jsonUpdate), the Phase 9 task table in the plan HTML, docs/adr/0001, packages/domain/src/**/models.ts, packages/db/src/schema.ts, packages/api/src/rate-limit.ts, apps/web/wrangler.jsonc, .github/workflows/preview.yml, packages/storage/**, TODOS.md.

Deliverables (each its own commit):
1. Model.Class: replace the hand-written row-shaped Schema classes in packages/domain (User, Post, UserPreferences, ApiKey, Workspace, WorkspaceMembership, WorkspaceInvite, ApplicationSettings, WaitlistEntry, BillingPlan, BillingPlanLimit, WorkspaceSubscription, UsageMeter, UsageRollup) with `Model.Class` declarations under `packages/domain/src/models/`; the contract exposes the `json` variant, services accept `insert`/`update`/`jsonCreate`/`jsonUpdate` where they fit; OpenAPI snapshot may change component names only — update the snapshot deliberately and diff it in the report. Parity test in packages/db (`src/__tests__/parity.test.ts`): every Drizzle column ↔ Model field name/nullability match (fail on either side drifting).
2. Rate limiting live layers: Cloudflare Rate Limiting binding (`ratelimits` in wrangler.jsonc: 60s windows) for scopes auth/contact/api-keys/operator-api; a D1-backed global counter table `rate_limit_window` (expand-only migration!) with a guarded upsert for signup and magic-link send; `RateLimiter` layer selection in apps/web runtime; pool-workers test hitting 429 through the binding (Miniflare supports ratelimit bindings — verify; else document); Cron Trigger `scheduled` runs a `Jobs` stub that prunes expired rate-limit rows (document the Queues follow-up in TODOS.md is already there).
3. Preview D1 per PR: preview.yml creates `gmacko-web-preview-<pr>` (`wrangler d1 create`), applies migrations, seeds, deploys with a generated wrangler env; close job deletes DB + Worker; weekly cron workflow lists `gmacko-web-preview-*` and prunes those without an open PR. Cannot be executed without Cloudflare credentials: write it, lint it (actionlint if available), and document manual verification steps.
4. Storage decision: keep UploadThing (default) — verify packages/storage works on Workers (fetch-based) with a unit test; document R2 as the alternative in packages/storage/README.md. (If the user later chooses R2, that is a separate task.)
5. Postgres → D1 data migration recipe for downstream apps: docs/legacy/postgres-to-d1.md + scripts/pg-to-d1.mjs (reads pg_dump --data-only or a live pg URL via `postgres`, emits INSERT statements per table mapping uuid→text, timestamps→epoch ms, booleans→0/1, json→text; applies via `wrangler d1 execute --file`); test converts the legacy seed dataset round-trip against sqlite-node (use packages/db layerTest) — the legacy seed SQL can be a fixture file since legacy-db is deleted in Phase 8.
6. `@effect/language-service` in tooling/typescript base tsconfig `plugins`; oxlint rules mirroring the new standards where expressible (or document why not).
7. Changeset for create-gmacko-app 0.2.1 with notes.
8. Gates: full `pnpm check`, `pnpm test:workers`, `pnpm e2e:web`. Do not push. Report per item, with anything unverifiable (credentials) called out.
