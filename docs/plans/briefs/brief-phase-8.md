# Phase 8 brief: scaffolder, docs, delete the legacy stack (cutover on the branch; merge is manual)

Read first: brief-common.md, the Phase 8 task table in the plan HTML (search "Phase 8 —"), packages/create-gmacko-app/src/**, packages/create-gmacko-app/src/__tests__/**, .github/workflows/cli-e2e.yml, docs/ai/SCAFFOLD_SPEC.md, AGENTS.md, CLAUDE.md, README.md, docs/ARCHITECTURE.md, docs/ai/DEVELOPER_EXPERIENCE.md, docs/DEPLOYMENT.md, docs/RUNBOOK.md, .claude/skills/create-gmacko-app-workflow (if vendored in repo; the user-level copy is at ~/.claude/skills/create-gmacko-app-workflow — report needed edits there, do not modify files outside the worktree), .claude/skills/bootstrap-saas, .claude/skills/launch-landing-page, TODOS.md, docs/adr/0001.

Deliverables:
1. Delete: apps/nextjs, packages/legacy-db, packages/legacy-api, packages/legacy-auth, packages/legacy-trpc-client (if it exists), docker-compose.yml, Dockerfile (root + any nextjs one), `.mcp.json` Next devtools entry, `@storybook/nextjs-vite`, `@t3-oss/env-nextjs`, `next`, `next-intl`, `@sentry/nextjs` catalog entries and any remaining imports; `portless.json` maps `gmacko.localhost` to apps/web only; root scripts drop `dev:next`, legacy db scripts, `db:legacy:*`; turbo `globalEnv` pruned of legacy vars. `grep -rn "next" --include=package.json` must be empty; `grep -rn "legacy-" --include='*.ts' --include='*.tsx' --include='*.json' --include='*.yml' --include='*.mjs'` (excluding docs/legacy and node_modules) must be empty.
2. Scaffolder (`packages/create-gmacko-app`): `PlatformConfig` = `{ web, mobile }` (drop `tanstackStart`), `--web` = apps/web (TanStack Start on D1), remove `vinext` option + `configureVinext` + its README; `--db` flag NOT added; prune rules: realtime is Node-only (default off; when enabled, keep it but warn), storage/email unchanged; rename every `apps/nextjs` reference to `apps/web`, `@gmacko/nextjs` → `@gmacko/web`; `webAppDir` logic; generated README/next-steps text (`pnpm dev`, `wrangler d1 create`, `pnpm -F @gmacko/db migrate:local`); `createForgeGraphConfig` emits the migrate-then-deploy stages; `customizeMcpConfig` no Next devtools; `provision.ts` if it references Postgres/Next; unit tests + snapshot fixtures updated; `cli-e2e.yml` matrix (web / mobile / both, presets) updated; run `pnpm -F create-gmacko-app test` and `RUN_E2E=true pnpm e2e:cli:full` (scaffolds a project, runs its check:fast) — must pass. Bump `create-gmacko-app` to 0.2.0 via a changeset with release notes (breaking: web lane is TanStack Start + Effect + D1; Next/tRPC/Postgres removed; API keys now scoped; operator tools need admin-scope keys).
3. `scripts/check-app-standards.mjs`: rules now enforced repo-wide (no legacy exclusions): `no-raw-process-env` (web-bundle scope), `no-cloudflare-env-outside-runtime`, `no-db-transaction`, `no-plain-drizzle-in-api`, `no-server-fn-for-data`, `no-dev-vars`, `endpoint-declares-credential` (every non-public endpoint in packages/domain has exactly one security middleware — can reuse the domain inspect script), `no-d1-table-rebuild`; keep `exact-host-check`, `no-committed-credentials`, `gate-debug-routes`, `no-partial-account-deletion` (re-point to `Account.deleteAccount` in packages/api); each rule has a fixture test.
4. Docs: AGENTS.md (invariants list updated: the new rules; local dev = emulate + portless + apps/web; D1), CLAUDE.md (UI workflow: `pnpm --filter @gmacko/ui storybook`; skills routing unchanged), docs/ARCHITECTURE.md (rewrite the diagram and package rules for domain/db/auth/api/api-client/web; request lifecycle = HttpApi middleware chain), docs/ai/DEVELOPER_EXPERIENCE.md (web stack = TanStack Start on Workers; remove vinext/Next lanes; support matrix updated), docs/ai/SCAFFOLD_SPEC.md (prompts/flags), README.md, CONTRIBUTING.md, docs/DEPLOYMENT.md, docs/RUNBOOK.md, docs/PACKAGE_EXTRACTION.md (rename references), move Next-specific docs (docs/legacy/*) and note the pg→d1 recipe lives in Phase 9. `.opencode/skill/*.md` references updated. Report the exact edits needed in `~/.claude/skills/create-gmacko-app-workflow/SKILL.md` (quick-reference table: apps/web, packages/domain, packages/api-client) — do not edit it.
5. Plan file: mark Phase 8 tasks done in docs/plans/2026-09-02-tanstack-effect-d1-migration.html (badges only; the orchestrator republishes).
6. Gates: `pnpm install`, `pnpm lint:ox`, `pnpm format:check`, `pnpm typecheck`, `pnpm check:standards`, `pnpm test`, `pnpm test:workers`, `pnpm build`, `pnpm e2e:web`, `pnpm -F create-gmacko-app test`, `RUN_E2E=true pnpm e2e:cli:full`, `pnpm knip` (fix findings in non-legacy code or add justified ignores), `pnpm check:release`. Commit in a few logical commits (delete, scaffolder, standards, docs). Do NOT merge into main and do NOT push; the merge is the user's call.

Report: deletion grep results, scaffolder test/e2e results, gate results, the list of user-level skill edits, and anything left.
